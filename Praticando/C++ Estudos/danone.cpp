#include <iostream>
#include <string>
#include "Danone2.cpp"

using namespace std;

/*
void é usada para indicar a "ausência de tipo" ou "ausência de valor".
Ele indica que uma função não retorna nenhum valor quando termina sua execução. A função simplesmente executa uma ação. */


int main () {

 
int i = Danone2( 3, 5 );

cout << "mensagem: " << i << endl;

cout << texto() << endl;

cout << endl;


    return 0;
}